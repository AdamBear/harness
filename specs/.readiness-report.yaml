status: approved
human_approval:
  status: approved
scope:
  spec: specs/19-living-wiki-intelligence-workspace.md
  plan: plans/living-wiki-intelligence-workspace-plan.md
summary:
  updated_at: "2026-05-05"
  purpose: "Refactor the Living Wiki Jaeger example into a research intelligence workspace and add executable MCP support."
blocking_decisions: []
inferred_defaults:
  - "MCP stdio clients are owned per local tool id for this wave; shared server pooling is deferred until a shared server config exists."
  - "MCP SDK loading is dynamic so TS-only harnesses do not load MCP code."
  - "Direct one-agent invocation is allowed through session.agents; multi-agent orchestration remains workflow-owned."
  - "Real OpenAI, draw.io MCP, and Jaeger checks are opt-in and skipped by default."
gate_simulation:
  status: passed
  notes:
    - "Plan now defines wave dependencies, ticket metadata, read/write scopes, verification, and non-overlap boundaries."
    - "Spec now defines direct agent invocation, review decisions, artifact manifests, graph payloads, MCP runtime semantics, and workflow output contracts."
    - "Human approval was granted on 2026-05-05 before implementation started."
deterministic_check:
  command: "node skills/spec-architect/scripts/check_specs.mjs specs"
  status: not_run
  reason: "No repo-local skills/spec-architect script path exists in this checkout."
confidence: medium
risks:
  - "MCP SDK transport APIs may require small implementation adjustments while preserving the locked public behavior."
  - "Existing code currently differs from the final MCP auth union and direct-agent spec updates; A0 must land before runtime tickets."
statistics:
  specs_updated: 8
  plans_added: 1
  new_spec_files: 1
